import { UpdateCategoryRequest } from './update-category-request';

describe('UpdateCategoryRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateCategoryRequest()).toBeTruthy();
  });
});
